{
  "name": "vpn-client",
  "version": "1.0.0",
  "description": "client designed to provide a gui which can create and destroy an openvpn connection",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nathan Marianovsky",
  "license": "MIT",
  "devDependencies": {
    "electron": "^22.0.0",
    "electron-installer-debian": "^3.1.0",
    "electron-packager": "^17.1.1",
    "electron-wix-msi": "^5.0.0"
  },
  "dependencies": {
    "electron-context-menu": "^3.6.1"
  }
}
